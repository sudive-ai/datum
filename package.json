{
  "name": "datum",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Every fact has a time and a place. A lightweight, event-sourced agent runtime.",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.30.1",
  "devDependencies": {
    "@sudive-ai/datum-workbench": "workspace:^",
    "@types/node": "^24.0.0",
    "typescript": "^5.9.0"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "demo": "node --experimental-strip-types --experimental-sqlite --env-file-if-exists=.env examples/hello-agent/start.ts"
  }
}
