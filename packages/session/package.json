{
  "name": "@datum-fw/session",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Append-only event log, derived projections, and fail-closed load — the single source of truth.",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "import": "./lib/index.js"
    }
  },
  "files": [
    "lib"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@datum-fw/kernel": "workspace:*"
  }
}
