{
  "name": "@datum-runtime/loop",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Agent contract and the default turn/step driver — the loop is a plugin, not the kernel.",
  "exports": {
    ".": {
      "types": "./lib/index.d.ts",
      "import": "./lib/index.js"
    }
  },
  "files": [
    "lib"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@datum-runtime/kernel": "workspace:*",
    "@datum-runtime/session": "workspace:*",
    "@datum-runtime/tools": "workspace:*"
  }
}
